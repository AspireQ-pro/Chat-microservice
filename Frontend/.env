VITE_API_URL=http://localhost:5000
VITE_SOCKET_URL=http://localhost:5000

# DEV ONLY — mock user injected when no ?token= is present
# Set VITE_DEV_USER_ID to a real user ID that exists in your chat DB
VITE_DEV_USER_ID=dev-user-001
VITE_DEV_USER_NAME=Dev User
VITE_DEV_USER_EMAIL=dev@example.com
